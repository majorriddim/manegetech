<!-- 画像部分ランダムにしたい -->

<link rel='stylesheet' href='css/questionindex.css' type='text/css' media='all' />


<h2><img src="<%= asset_path "title.png" %>"/></h2>


<% @category.categories.each do |category| %>
<ul>
<li class="questionindex">・<%= link_to category.name, category_questions_path(category), :class => 'questionindex' %></li>
</ul>
<% end %>

<% @questions.each do |question| %>
<ul>
  <li class="questionindex">・<%= link_to question.title, question, :class => 'questionindex'  %></li>
</ul>
<% end %>
